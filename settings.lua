require("constants")

data:extend({
    {
        type = "bool-setting",
        name = USE_MAP_GEN_CLIFFS_SETTING,
        setting_type = "startup",
        default_value = false,
        order = "a[use-map-gen-cliffs]"
    }
})
